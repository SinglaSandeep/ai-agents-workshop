# Zava AI Agents Workshop — pinned dependencies.
# Install with:
#   python -m pip install --pre -r requirements.txt
# (--pre is needed because a few agent-framework packages are still pre-release.)

# --- Chat app + config ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
httpx==0.28.1
requests==2.32.3

# --- Azure SDKs ---
azure-identity==1.19.0
azure-cosmos==4.9.0
azure-ai-projects==2.0.0b3
azure-core==1.32.0
openai==1.57.4

# --- MCP servers (Exercises 02 & 04) ---
mcp==1.4.0
fastmcp==2.5.0

# --- Microsoft Agent Framework (Exercises 01, 03, 06, 07) ---
agent-framework-core==1.6.0
agent-framework-foundry==1.6.0
agent-framework-orchestrations==1.0.0rc2
agent-framework-devui==1.0.0b260521

# --- Foundry-hosted Marketing agent (Exercise 05) ---
agent-framework-foundry-hosting==0.3.0a0

# --- Observability (Exercise 11) ---
azure-monitor-opentelemetry==1.6.4
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-httpx==0.50b0

# --- Evaluations + red teaming (Exercises 09 & 10) ---
azure-ai-evaluation[redteam]==1.0.1
rich==13.9.4

# --- Dev tooling ---
ruff==0.8.4
